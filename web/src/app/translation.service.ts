import { Injectable } from '@angular/core';
import { TranslationSharedService } from './translation-shared.service';

@Injectable({
  providedIn: 'root',
})
export class TranslationService extends TranslationSharedService {
}
